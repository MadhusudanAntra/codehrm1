apiVersion: v1
kind: Service
metadata:
    name: hrmmicroservicesimagecontainerregistery
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: hrmmicroservicesimagecontainerregistery